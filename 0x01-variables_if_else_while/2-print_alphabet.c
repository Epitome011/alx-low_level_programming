#include <stdio.h>
#include <stdlib.h>

/**
 * main - returns alphabets
 *
 * Return: 0 (Success)
 */
int main(void)
{
	int ch = 'a';

	while (ch <= 'z')
	{
		putchar(char);
		ch++;
	}
	putchar('\n');
	return (0);
}
